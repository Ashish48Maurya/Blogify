<!DOCTYPE html>
<html lang="en">
{{>link}}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            margin: 20px;
        }

        .col-md-6 {
            margin-bottom: 20px;
        }

        .profile-photo img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
        }

        .Image {
            max-width: 100%;
            height: auto;
        }

        .message-section {
            margin-bottom: 30px;
        }

        .message-section h2 {
            margin-bottom: 10px;
        }

        .message-input-section {
            margin-top: 20px;
            display: flex;
            align-items: center;
        }

        .message-input-section .message-input {
            flex: 1;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            width: 100%;
        }

        .join {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }

        @media (min-width: 768px) {
            .join {
                margin-left: 300px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="col-md-6">
            <div class="profile-photo">
                <img src="{{blog.creator.profileImg}}" alt="Profile Photo" class="img-fluid">
                <h4>Posted By: {{blog.creator.Username}}</h4>
            </div>
            <img class="Image" src="{{blog.coverImg}}" alt="Image">
        </div>

        <div class="col-md-6">
            <h4>{{blog.title}}</h4>
            <p>{{blog.body}}</p>
        </div>
    </div>

    <div class="message-input-section">
        <form action="/blogPage/chats/{{blog._id}}" method="POST">
            <div class="join">
                <input type="text" class="message-input form-control" name="content" placeholder="Enter your message">
                <button type="submit" class="btn btn-primary">Send</button>
            </div>
        </form>
    </div>

    {{#each comments}}
    <div class="join">
        <img src="{{this.createdBy.profileImg}}" alt="Profile Photo" class="img-fluid">
        {{!-- <img src="{{this.createdBy.Username}}" alt="Profile Photo" class="img-fluid"> --}}
        <h5>{{this.createdBy.Username}}</h5>
        <h1>Comment: </h1>
        <h4>{{this.content}}</h4>
    </div>
    {{/each}}

</body>
{{>script}}
</html>
